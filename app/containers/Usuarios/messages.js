/*
 * Usuarios Messages
 *
 * This contains all the text for the Usuarios container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Usuarios';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Usuarios container!',
  },
});
