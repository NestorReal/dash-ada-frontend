/*
 * Perfil Messages
 *
 * This contains all the text for the Perfil container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Perfil';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Perfil container!',
  },
});
