// import { selectComportamientoDomain } from '../selectors';

describe('selectComportamientoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
