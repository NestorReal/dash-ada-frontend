/*
 * Credito Messages
 *
 * This contains all the text for the Credito container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Credito';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Credito container!',
  },
});
