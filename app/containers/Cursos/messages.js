/*
 * Cursos Messages
 *
 * This contains all the text for the Cursos container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Cursos';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Cursos container!',
  },
});
